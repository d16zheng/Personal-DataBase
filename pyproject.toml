[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "personal-db"
version = "0.1.0"
description = "A personal database project that starts as an in-memory key-value store."
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
